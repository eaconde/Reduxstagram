import React from 'react';

const Photo = React.createClass({
  render () {
    return (
      <div className="single-photo">
        single photo view
      </div>
    );
  }
});

export default Photo;